<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="product">

	<select id="select" parameterType="int" resultType="productDto">
		SELECT PRODUCTSEQ, CATEGORY, PRODUCTNAME, STARTDATE, ENDDATE, THUMBIMG, PRODUCTCONTENT, AREACODE, ADDRESS, TELLNUMBER, RUNTIME, RATING, LIKECOUNT, WISHCOUNT, PRODUCTCOUNT
		FROM PRODUCT
		WHERE AREACODE = #{areaCode}
	</select>

	<insert id="insert" parameterType="productDto">
		INSERT INTO PRODUCT
		(PRODUCTSEQ, CATEGORY, PRODUCTNAME, STARTDATE, ENDDATE, THUMBIMG, PRODUCTCONTENT, AREACODE, ADDRESS, TELLNUMBER, RUNTIME, RATING)
		VALUES 
		(PRODUCTSEQ.NEXTVAL, #{category}, #{productname}, #{startdate}, #{enddate}, #{thumbimg}, #{productcontent}, #{areacode}, #{address}, #{tellnumber}, #{runtime}, #{rating})
	</insert>

</mapper>